return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    close_if_last_window = true,
    buffers = {
      follow_current_file = {
        enabled = false,
        leave_dirs_open = false,
      },
    },
  },
}
